#include <stdio.h>

int main() {
    int N;
    long long sum = 0, x;

    scanf("%d", &N);

    for(int i = 0; i < N; i++) {
        scanf("%lld", &x);
        sum += x;
    }

    double average = (double)sum / N;
    printf("%.6f", average);

    return 0;
}
