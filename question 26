#include <stdio.h>

int main() {
    int A, B;
    scanf("%d", &A);
    scanf("%d", &B);

    // Swap using XOR
    A = A ^ B;
    B = A ^ B;
    A = A ^ B;

    printf("%d\n", A);
    printf("%d", B);

    return 0;
}
