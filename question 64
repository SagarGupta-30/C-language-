#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    int count = 0;
    int i = 1;

    while (count < N) {
        int square = i * i;

        if (square % 3 != 0) {
            printf("%d ", square);
            count++;
        }

        i++;
    }

    return 0;
}
